<?php

/**
 * @file
 * Install hooks for tiles_layout module.
 */

/**
 * Implements hook_schema().
 */
function tiles_layout_schema() {
  $schema['tiles_layout'] = array(
    'description' => 'Share layout entities for pages implementing tiles',
    'fields' => array(
      'id' => array(
        'description' => 'The primary identifier for this layout entity.',
        'type' => 'serial',
        'unsigned' => TRUE,
        'not null' => TRUE,
      ),
      'label' => array(
        'description' => 'The title/label of this layout.',
        'type' => 'varchar',
        'length' => 255,
        'not null' => TRUE,
        'default' => '',
      ),
      'context' => array(
        'description' => 'The {context}.name this layout implements.',
        'type' => 'varchar',
        'length' => 255,
        'not null' => TRUE,
        'default' => '',
      ),
      'status' => array(
        'type' => 'int',
        'not null' => TRUE,
        // Set the default to ENTITY_CUSTOM without using the constant as it is
        // not safe to use it at this point.
        'default' => 0x01,
        'size' => 'tiny',
        'description' => 'The exportable status of the entity.',
      ),
      'module' => array(
        'description' => 'The name of the providing module if the entity has been defined in code.',
        'type' => 'varchar',
        'length' => 255,
        'not null' => FALSE,
      ),
    ),
    'foreign keys' => array(
      'layout_context' => array(
        'table' => 'context',
        'columns' => array('context' => 'name'),
      ),
    ),
    'primary key' => array('id'),
  );
  return $schema;
}
